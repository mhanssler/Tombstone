# Supabase frontend configuration (optional; app still works offline without these)
# Get values from: Supabase Dashboard -> Project Settings -> API
#
# SAFE in frontend (Vercel + browser):
# - VITE_SUPABASE_URL
# - VITE_SUPABASE_ANON_KEY
#
# DO NOT put SUPABASE_SERVICE_ROLE_KEY in this file.
# Service role belongs only in server-side contexts (for example scripts/.env.owlet-bridge).
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
